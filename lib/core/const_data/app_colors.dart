import 'package:flutter/material.dart';

class ColorsManager {
  static const Color lightThemeColor = Colors.red,
      white = Colors.white,
      black = Colors.black,
      darkThemeColor = Colors.yellow,
      scaffoldBackColor = Colors.black12,
      primary = Colors.amber,
      grey = Colors.grey;
  static const Color backgroundColor = Color.fromARGB(255, 117, 117, 117);
  static const Color foregroundColor = Color.fromARGB(255, 189, 189, 189);
}
