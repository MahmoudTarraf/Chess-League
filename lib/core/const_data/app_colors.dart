import 'package:flutter/material.dart';

class ColorsManager {
  static const Color lightThemeColor = Colors.red,
      white = Colors.white,
      black = Colors.black,
      darkThemeColor = Colors.yellow,
      scaffoldBackColor = Color.fromARGB(255, 117, 117, 117),
      primary = Colors.amber,
      grey = Colors.grey;
  // static const Color backgroundColor = Color.fromARGB(255, 117, 117, 117);
  static const Color backgroundColor = Color.fromARGB(255, 172, 109, 86);
  // static const Color foregroundColor = Color.fromARGB(255, 189, 189, 189);
  static const Color foregroundColor = Colors.white,
      backButton = Colors.black12;
}
